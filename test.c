/*
 * This source was automatically generated by Brainfuck Compiler C
 */
#include <stdio.h>
#include <stdlib.h>

int main() {
	char buffer[30000];
	char* memory = buffer;
	memory++;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	while(*memory){
	memory--;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	memory++;
	--*memory;
	}
	memory--;
	putchar(*memory);
	memory++;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	while(*memory){
	memory--;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	memory++;
	--*memory;
	}
	memory--;
	++*memory;
	putchar(*memory);
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	putchar(*memory);
	putchar(*memory);
	++*memory;
	++*memory;
	++*memory;
	putchar(*memory);
	memory++;
	memory++;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	while(*memory){
	memory--;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	memory++;
	--*memory;
	}
	memory--;
	++*memory;
	++*memory;
	putchar(*memory);
	--*memory;
	--*memory;
	--*memory;
	--*memory;
	--*memory;
	--*memory;
	--*memory;
	--*memory;
	--*memory;
	--*memory;
	--*memory;
	--*memory;
	putchar(*memory);
	memory++;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	while(*memory){
	memory--;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	memory++;
	--*memory;
	}
	memory--;
	++*memory;
	putchar(*memory);
	memory--;
	putchar(*memory);
	++*memory;
	++*memory;
	++*memory;
	putchar(*memory);
	--*memory;
	--*memory;
	--*memory;
	--*memory;
	--*memory;
	--*memory;
	putchar(*memory);
	--*memory;
	--*memory;
	--*memory;
	--*memory;
	--*memory;
	--*memory;
	--*memory;
	--*memory;
	putchar(*memory);
	memory++;
	memory++;
	memory++;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	while(*memory){
	memory--;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	++*memory;
	memory++;
	--*memory;
	}
	memory--;
	++*memory;
	putchar(*memory);
	putchar(*memory);
	--*memory;
	putchar(*memory);
	putchar(*memory);
	putchar(*memory);
	putchar(*memory);
	putchar(*memory);
	putchar(*memory);
	putchar(*memory);
	putchar(*memory);
	putchar(*memory);
	putchar(*memory);
	putchar(*memory);
	putchar(*memory);
	putchar(*memory);
	putchar(*memory);
	putchar(*memory);
	*memory=getchar();
	putchar(*memory);
	--*memory;
	--*memory;
	exit(0);
	return 0;
}
